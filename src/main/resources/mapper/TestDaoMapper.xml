<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.demomybatis.dao.TestDao">
    <!-- 根据主键查询-->
    <select id="getById" resultType="com.example.demomybatis.dto.EntityDTO" parameterType="java.lang.Integer">
        select *
        from mytable
        where id = #{id}
    </select>
</mapper>