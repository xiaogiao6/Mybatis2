<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dao.IUserDao">
    <resultMap id="userAccountMap" type="com.domain.User">
        <id property="id" column="id"></id>
        <result property="username" column="username"></result>
        <result property="address" column="address"></result>
        <result property="sex" column="sex"></result>
        <result property="birthday" column="birthday"></result>
        <!--配置user对象accounts集合-->
        <collection property="accounts" ofType="account" select="com.dao.IAccountDao.findById" column="id"></collection>
    </resultMap>
     <!--配置查询所有用户-->
    <select id="findAll" resultMap="userAccountMap">
        SELECT * FROM user u left outer join account a on u.id=a.uid
    </select>
    <insert id="saveUser" parameterType="com.domain.User">
        <selectKey keyProperty="id" keyColumn="id" resultType="int" order="AFTER">
            select last_insert_id();
        </selectKey>
        insert into user(username,address,sex,birthday)values(#{username},#{address},#{sex},#{birthday});
    </insert>
    <update id="updateUser" parameterType="com.domain.User">
        update user set username=#{username},address=#{address} where id=#{id}
    </update>
    <delete id="deleteUser" parameterType="java.lang.Integer">
        delete from user where id= #{userId}
    </delete>
    <select id="findOneUser" parameterType="java.lang.Integer"  resultType="com.domain.User">
        SELECT * FROM user where id=#{uid}
    </select>
    <select id="findByname" parameterType="String"  resultType="com.domain.User">
        SELECT * FROM user where username like #{name}
    </select>
    <!-- 根据条件查询-->
    <select id="findByCondition"  parameterType="com.domain.User" resultType="com.domain.User">
    select * from user
     <where>
        <if test="username!=null">
           and username=#{username}
        </if>
        <if test="sex!=null">
            and sex=#{sex}
        </if>
     </where>

    </select>
<!--根据Id集合查询-->
    <select id="findUserInIds" resultType="com.domain.User" parameterType="com.domain.QueryVo">
          select * from user
          <where>
              <if test="ids != null and ids.size()>0">
                  <foreach collection="ids" open="and id in(" close=")" item="id" separator=",">
                     #{id}
                  </foreach>

              </if>
          </where>
    </select>



</mapper>
